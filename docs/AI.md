# CodeLens AI

The AI system will eventually support:

- Repository question answering
- Code explanation
- Architecture explanation
- Finding explanation
- Suggested improvements

AI should receive relevant repository context rather than the entire repository.

The AI provider should be abstracted so the application is not permanently tied to one model provider.